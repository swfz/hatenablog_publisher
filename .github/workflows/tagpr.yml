name: tagpr

on:
  push:
    branches: ["master"]
jobs:
  tagpr:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
    - uses: Songmu/tagpr@242e22916eb775d6e359959fe3ba5c497de4a01a # v1
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
